# ============================================
# SERVER ENVIRONMENT CONFIGURATION
# ============================================
# Copy this file to .env and fill in your values
# NEVER commit the .env file to version control!

# --------------------------------------------
# Server Configuration
# --------------------------------------------
NODE_ENV=development
PORT=5000

# --------------------------------------------
# MongoDB Database Configuration
# --------------------------------------------
# Replace with your MongoDB connection string
# For production, use strong credentials and whitelist IPs
MONGODB_URI=mongodb+srv://mahar:<db_password>@project.e5k7hmj.mongodb.net/dailyblogs?retryWrites=true&w=majority&appName=Project

# --------------------------------------------
# Session Configuration
# --------------------------------------------
# IMPORTANT: Generate a strong secret for production!
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
SESSION_SECRET=CHANGE_THIS_TO_A_STRONG_64_CHARACTER_RANDOM_STRING

# Session expiry in milliseconds (7 days = 604800000)
SESSION_MAX_AGE=604800000

# --------------------------------------------
# Cookie Configuration
# --------------------------------------------
# Set to true in production with HTTPS (Render sets NODE_ENV=production automatically)
COOKIE_SECURE=false

# Cookie domain (leave empty for localhost)
COOKIE_DOMAIN=

# --------------------------------------------
# CORS Configuration
# --------------------------------------------
# Frontend URL(s) for CORS whitelist (comma-separated)
# Locally: http://localhost:5173
# Production: your Vercel URL, e.g. https://daily-blogs.vercel.app
CORS_ORIGIN=http://localhost:5173

# --------------------------------------------
# Admin Configuration
# --------------------------------------------
# Default admin email pattern (users with this in email get admin role)
ADMIN_EMAIL_PATTERN=admin

# Super Admin Configuration (exact email - only this user can manage users and contacts)
SUPER_ADMIN_EMAIL=shabbirsohail33@gmail.com

# --------------------------------------------
# Email Configuration (for password reset, OTP, etc.)
# --------------------------------------------
# For Gmail, use App Password (not your regular password)
# Generate App Password: https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-gmail-app-password-here

# --------------------------------------------
# Security Configuration
# --------------------------------------------
# Bcrypt salt rounds (higher = more secure but slower)
BCRYPT_SALT_ROUNDS=12

# Rate limiting enabled (true/false)
RATE_LIMIT_ENABLED=true

# --------------------------------------------
# Chat Encryption
# --------------------------------------------
# 32-char hex key for AES-256-GCM chat message encryption
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex').slice(0,32))"
CHAT_ENCRYPTION_KEY=CHANGE_THIS_TO_A_32_CHARACTER_HEX_STRING
